const host = 'exp.host';
const origin = `https://${host}`;
const websiteOrigin = 'https://expo.dev';

export const Config = {
  api: {
    host,
    origin,
  },
  website: {
    origin: websiteOrigin,
  },
};
